export declare type InfoCardType = {
  rating: number;
  routes: string;
  difficulties: string;
  classes: string;
  type: "crag" | "sector";
};

export declare type onClickType = (arg0: boolean) => any;

export {};
